# Copy this file to .streamlit/secrets.toml and fill in the values.
# secrets.toml is gitignored — never commit it.

[database]
url = "postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres"

[auth]
cookie_name = "fleet_scheduler"
cookie_key  = "REPLACE_WITH_RANDOM_32_CHAR_STRING"

# Add one block per user.
# Generate password hashes with: python generate_hash.py

[credentials.usernames.maxime]
name     = "Maxime Gosselin"
password = "REPLACE_WITH_BCRYPT_HASH"

[credentials.usernames.saito]
name     = "Saito"
password = "REPLACE_WITH_BCRYPT_HASH"
